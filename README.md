# self-pruning-network
Self-pruning neural network in PyTorch using learnable gates and L1 regularization to remove unnecessary weights, demonstrating the trade-off between accuracy and model sparsity on CIFAR-10.
